package org.example.repository;

import org.example.jpa.RoutineMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoutineMapRepository extends JpaRepository<RoutineMap, Long> {
}
